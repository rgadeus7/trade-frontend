import type { Metadata, Viewport } from 'next'
import { Inter } from 'next/font/google'
import './globals.css'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'Trading Dashboard - Real-time Market Analytics',
  description: 'Professional trading dashboard with real-time market data, portfolio tracking, and advanced analytics.',
  keywords: 'trading, dashboard, stocks, crypto, portfolio, analytics',
  authors: [{ name: 'Trading Dashboard Team' }],
}

export const viewport: Viewport = {
  width: 'device-width',
  initialScale: 1,
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>
        <div className="min-h-screen bg-gray-50">
          {children}
        </div>
      </body>
    </html>
  )
}
